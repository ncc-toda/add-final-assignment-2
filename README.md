# weather

気象庁(JMA)非公式JSON APIを利用した、ターミナル上で完結する天気予報TUIツール(Rust製)。

- 開発中(MVP実装フェーズ)。詳細は `docs/requirements.md` を参照。
- 実装計画・進捗は `docs/plans/mvp_plans.md` を参照。

## ビルド

```sh
cargo build
```

## 実行

```sh
cargo run
```

(コマンド体系・設定ファイルの説明は後続タスクで追記予定)

## テスト

```sh
cargo test
```
